/**
 * Shared constants. In this MVP a single demo organization is used for all
 * requests; multi-tenant auth resolves ctx.orgId to real orgs in production.
 */
export const DEFAULT_ORG_ID = 'org_demo';
export const DEFAULT_USER_EMAIL = 'owner@demo.voxdesk.local';
